export function parallelAllStartGateway(data, done) {
  done(data);
}

export function joinCapabilityAndFeatureSetGateway(data, done) {
  done(data);
}

export function parallelAllEndGateway(data, done) {
  done(data);
}
