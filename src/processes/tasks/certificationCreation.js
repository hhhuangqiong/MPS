// for each supported platforms
// - get certificate templates
// - create certificate with template, application id? why save application?

export default function certificationCreation(data, done) {
  done(data);
}
