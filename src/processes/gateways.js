export function PARALLEL_ALL_START(data, done) {
  done(data);
}

export function JOIN_CAPABILITY_AND_FEATURE_SET(data, done) {
  done(data);
}

export function PARALLEL_ALL_END(data, done) {
  done(data);
}
